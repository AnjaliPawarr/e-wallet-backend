server:
  port: 8090

spring:
  application:
    name: wallet-service

  datasource:
    url: jdbc:mysql://localhost:3306/ewallet_wallet
    username: root
    password: 25082003@anjali
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/